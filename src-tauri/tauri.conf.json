{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "BBDG HWP Editor",
  "version": "0.7.111",
  "identifier": "com.bbdg.hwp-editor",
  "build": {
    "beforeDevCommand": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"& { $candidates = @('./src-tauri/ensure-vite-dev-server.ps1', './ensure-vite-dev-server.ps1'); foreach ($p in $candidates) { if (Test-Path $p) { & $p; exit $LASTEXITCODE } }; throw 'ensure-vite-dev-server.ps1 not found' }\"",
    "beforeBuildCommand": "powershell -NoProfile -Command \"Set-Location '../rhwp-studio'; npm run build\"",
    "devUrl": "http://localhost:7700",
    "frontendDist": "../rhwp-studio/dist"
  },
  "app": {
    "windows": [
      {
        "title": "BBDG HWP Editor",
        "width": 1200,
        "height": 800,
        "dragDropEnabled": false
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "icon": ["icons/icon.ico"],
    "publisher": "BBD Global Co., Ltd.",
    "windows": {
      "wix": {
        "language": ["ko-KR"]
      },
      "nsis": {
        "languages": ["Korean"]
      }
    }
  }
}
