# 타스크 43 - 단계 2 완료보고서

## 단계 정보

| 항목 | 내용 |
|------|------|
| 타스크 | #43 한컴 웹기안기 대응 기능정의서 작성 |
| 단계 | 2/3 - rhwp API 매핑 및 Gap 분석 |
| 완료일 | 2026-02-12 |

## 수행 내용

### rhwp WASM API 전수 조사

`src/wasm_api.rs`를 분석하여 rhwp의 전체 WASM API를 확인하였다:
- HwpDocument: 50개 메서드 (12개 카테고리)
- HwpViewer: 8개 메서드 (뷰어 전용)
- 합계: 58개 공개 API

### 호환성 매핑 수행

웹기안기 HwpCtrl API 전체(Properties 18, Methods 67, Actions 314, ParameterSets 50)를 rhwp API에 매핑하고 호환성 등급(A~X)을 부여하였다.

### 핵심 발견사항

**아키텍처 차이점**:
- 한컴: **커서 기반** API (커서 이동 → 현재 위치에서 편집)
- rhwp: **좌표 기반** API (section/para/offset 직접 접근)
- 호환 레이어에서 JS 커서 상태 관리 시스템이 필수

### Gap 분석 결과

| 등급 | 수 | 비율 | 의미 |
|------|-----|------|------|
| A (직접 매핑) | 5 | 1% | 래퍼만 작성 |
| B (변환 매핑) | ~115 | 26% | 파라미터 변환 래퍼 |
| C (신규 구현) | ~290 | 65% | WASM 코어 구현 필요 |
| D (아키텍처 차이) | 2 | 0.4% | 구조적 재설계 |
| X (스텁 처리) | ~37 | 8% | 빈 함수 |
| **합계** | **449** | **100%** | |

**신규 구현 필요 핵심 영역**:
1. 커서/위치 시스템 (96개 API)
2. 필드 시스템 (13개 API) — 공공기관 마이그레이션 핵심
3. 개체 조작 (62개 API)
4. Action/ParameterSet 시스템 (19개 API)
5. 검색/치환 (9개 API)
6. Undo/Redo (2개 API)

## 산출물

| 문서 | 경로 | 작성 내용 |
|------|------|----------|
| 기능정의서 Section 3 | `mydocs/plans/task_43_feature_def.md` | rhwp vs 웹기안기 호환성 매핑표 (아키텍처 차이, rhwp API 목록, Properties/Methods/Actions/ParameterSets 등급별 상세 매핑, 전체 통계) |

## 다음 단계

- **단계 3**: 미구현 기능 우선순위 분류(P0/P1/P2), 마이그레이션 시나리오별 필요 API, rhwp 고유 강점, 호환 레이어 구현 로드맵
- 산출물: 기능정의서 Section 4~7
