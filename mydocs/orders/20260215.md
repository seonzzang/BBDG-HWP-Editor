# 오늘 할일 - 2026년 2월 15일

| No | 타스크 | 상태 | 비고 |
|----|--------|------|------|
| 76 | 이미지 배치 로직 근본 원인 분석 및 체계화 | 완료 | compute_object_position() 통합 함수 추출. layout_body_picture/layout_shape/calculate_shape_reserved_height 3개 함수 리팩토링. VertRelTo::Page, HorzRelTo::Para, Paper 바이패스 불일치 수정. 491개 Rust 테스트 통과, WASM/Vite 빌드 성공, 웹 검증 완료 |
| 77 | 페이지 하단 표 셀 내 이미지 처리 | 완료 | 이미지 셀 행의 인트라-로우 분할 금지. is_row_splittable() 메서드 추가. 492개 Rust 테스트 통과, WASM/Vite 빌드 성공, 웹 검증 완료 |
| 78 | 20250130-hongbo.hwp 2페이지 이미지 2개 렌더링 누락 | 완료 | 파서의 구버전 Group 감지 조건 오류 수정. Rectangle+TextBox가 Group으로 오인식되던 문제 해결. 493개 Rust 테스트 통과, WASM/Vite 빌드 성공 |
| 79 | 표내 셀에 캐럿 위치 시 투명선 보여주기 | 완료 | 선 종류가 "선 없음"인 표 테두리를 빨간색 점선으로 표시. 보기 메뉴 토글 구현. 494개 Rust 테스트 통과, WASM/Vite 빌드 성공, 웹 검증 완료 |
| 80 | 표 셀 높이를 HWP와 동일하게 처리 | 완료 | 마지막 줄 line_spacing 제외로 셀 높이 정확도 개선(3곳). 셀 렌더링 trailing spacing 조건부 제거로 텍스트 오버플로우 해결. 세로 중간정렬 미묘한 비대칭은 다음 타스크로 이관. 495개 Rust 테스트 통과, WASM/Vite 빌드 성공 |
| 81 | 표 셀 세로쓰기 구현 | 완료 | text_direction != 0인 셀의 세로쓰기 레이아웃 구현. 괄호 CW 90° 회전(중앙 정렬 기반 렌더링, advance 축소), 캐럿 진입/이동(화살표 키 교체), 문단부호 표시+회전. 496개 Rust 테스트 통과, WASM/Vite 빌드 성공, 웹 검증 완료 |
| 82 | 컨텍스트 메뉴 인프라 + 표 우클릭 메뉴 | 완료 | ContextMenu 클래스 신규, 표 셀 내 우클릭 시 표 편집 메뉴, 표 밖 일반 편집 메뉴, 브라우저 기본 메뉴 억제, ESC/외부클릭 닫기, table 커맨드 canExecute 활성화. 496개 Rust 테스트 통과, WASM/Vite 빌드 성공, 웹 검증 완료 |
| 83 | F5 셀 선택 모드 + 셀 범위 선택 | 완료 | F5 셀 선택 모드, 화살표 이동, Shift+클릭 범위선택, Ctrl+클릭 제외 토글, 우클릭 영역유지. 496개 Rust 테스트 통과, WASM/Vite 빌드 성공, 웹 검증 완료 |
| 84 | 표/셀 속성 다이얼로그 | 완료 | 6탭 다이얼로그(표/셀 탭 활성, 기본/여백/테두리/배경 비활성), WASM API 4개 추가. 496개 Rust 테스트 통과, WASM/Vite 빌드 성공, 웹 검증 완료 |
| 85 | 셀 병합/나누기 | 완료 | 셀 범위 병합(M), 단일 셀 나누기(S) 기능 구현. WASM 브릿지 연결, InputHandler 메서드 3개 추가. Vite 빌드 성공, 웹 검증 완료 |
| 86 | 행/열 추가 및 삭제 | 완료 | Rust delete_row/delete_column + WASM API + 브릿지 + 커맨드 6개 구현 + Alt+Insert/Delete 단축키. 511개 Rust 테스트 통과, WASM/Vite 빌드 성공, 웹 검증 완료 |
| 87 | 표 객체 선택 + 시각적 피드백 | 완료 | Esc 키 표 객체 선택, 파란 테두리+8핸들 시각 피드백, Delete 삭제, 리사이즈 커서, 투명선 자동 ON/OFF, table:delete 커맨드+컨텍스트 메뉴. 514개 Rust 테스트 통과, WASM/Vite 빌드 성공 |
| 88 | 표 구조 변경 후 저장 시 HWP 파일 손상 수정 | 완료 | B6 해결. new_from_template() has_para_text=false, char_count=1. new_empty() cc=1+기본LineSeg. 515개 Rust 테스트 통과, WASM/Vite 빌드 성공 |
| 89 | HWPX 파일 처리 지원 | 완료 | HWPX(ZIP+XML) 파서 5개 모듈 구현. header.xml→DocInfo, section*.xml→Section, content.hpf→섹션목록, BinData 로딩. 포맷 자동감지, WASM/프론트엔드 통합. lineseg/landscape/strikeout/표셀속성 수정. 529개 Rust 테스트 통과, WASM/Vite 빌드 성공, SVG 내보내기 검증 완료 |
| 90 | HWPX 파서 정확도 개선 (python-hwpx 참조) | 완료 | utils.rs 공통모듈 추출, charPr emboss/engrave, paraPr breakSetting/autoSpacing/border offset, 이미지 크기 0×0 해결(orgSz 폴백), pos/margin/clip 파싱, 글꼴 언어 7그룹 매핑, borderFill gradation 색상/imgBrush/slash, columnBreak, cellPr 파싱. 532개 Rust 테스트 통과, WASM/Vite 빌드 성공, 5개 HWPX 샘플 SVG 검증 완료 |
| 91 | 다단(multi-column) 레이아웃 처리 | 완료 | ColumnDef 파서 비트필드 수정(스펙 표 141), ColumnDef 추출+페이지네이션 연결, MultiColumn/Column 나누기 처리, 4곳 다단 플로우 수정, 단 구분선 렌더링, HWPX colPr/columnBreak 파싱. treatise sample 17→9페이지. 532개 Rust 테스트 통과, WASM/Vite 빌드 성공 |

## 백로그

| No | 내용 | 발견일 | 비고 |
|----|------|--------|------|
| B3 | ~~이미지 배치 로직 근본 원인 분석 및 체계화~~ | 2026-02-15 | **타스크 76으로 해결 완료** |
| B4 | ~~20250130-hongbo.hwp 2페이지 그룹 이미지 3장 페이지 분산 문제~~ | 2026-02-15 | **해결 완료** |
| B5 | ~~표 셀 세로 중간 정렬 시 baseline 비대칭 보정~~ | 2026-02-15 | **해결 완료** |
| B6 | ~~표 구조 변경 후 저장 시 HWP 프로그램 파일 손상 오류~~ | 2026-02-15 | **타스크 88으로 해결 완료** |
| B7 | 표 리팩토링 (5단계: Dense Grid → 통합 레이아웃 → Path 기반 접근 → Prefix Sum 페이지네이션 → 증분 리플로우) | 2026-02-15 | 계획서: `mydocs/plans/table_refactoring_plan.md` |
