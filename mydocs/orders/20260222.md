# 오늘 할일 - 2026년 2월 22일

| No | 타스크 | 상태 | 비고 |
|----|--------|------|------|
| 139 | 그림 선택 + 속성 대화상자 구현 | 완료 | 그림 클릭 선택(8핸들+외곽선 마킹), Delete 삭제, 우클릭 컨텍스트 메뉴, 개체 속성 대화상자(한컴 UI 8탭), 개체 설명문 서브 대화상자, 자동 설명문 생성, 핸들 드래그 리사이즈 |
| 140 | 그림 이동 기능 + HWP 저장 호환성 | 완료 | 키보드 방향키 이동(3mm 단위), 마우스 드래그 이동, MovePictureCommand(Undo 지원), move 커서, treat_as_char 이동 불가. HWP 저장 호환성: prevent_page_break 필드 추가, SHAPE_COMPONENT ctrl_id/렌더링행렬, SHAPE_PICTURE border/crop/extra, PARA_LINE_SEG 이미지 크기, CommonObjAttr 크기기준 비트, extract_str JSON 이스케이프 |
| 141 | build_page_tree 캐시로 hitTest/getPageControlLayout 성능 개선 | 완료 | 타스크 137 회귀 원인 분석(이진탐색), build_page_tree_cached 도입(RefCell+Vec 캐시), PageRenderTree 등 15개 타입 Clone derive, 문서 변경 시 캐시 무효화(paginate/recompose_*/insert/remove), hit_test_native·get_page_control_layout_native·get_table_bbox_native 캐시 적용, Docker cargo-bin 볼륨 추가 |
| 142 | 코드베이스 리팩토링 (1,200줄 상한 파일 분할) | 완료 | 6단계 완료. wasm_api(24,586→1,839+12모듈), layout(8,709→1,128+10모듈), TS/CSS 분할. 테스트 582개 통과, Clippy 0 |
| 143 | Lazy Pagination (mark_dirty + ensure_paginated) | 완료 | P0. paginate() 45회 분산 호출 → mark_dirty()+ensure_paginated() 지연 실행. 582테스트 통과 |
| 144 | JSON 유틸리티 통합 | 완료 | P0. 중복 파싱 함수 14개 제거(5모듈), json_escape() 11곳 통합, json_ok_with() 23곳 적용. Dockerfile Clippy 추가 |
| 145 | ShapeObject::common() 활용 확대 | 완료 | P0. shape_attr()/shape_name() 추가, z_order() 단순화, 8곳 match 블록 제거(-92줄/+46줄) |
| 146 | 거대 함수 분해 | 완료 | P1. build_render_tree 921→72줄, paginate_with_measured 1,455→120줄, layout_table 1,002→158줄. PaginationState 구조체 도입. 31개 메서드 추출 |
| 147 | CQRS Command/Query 파일 분리 | 완료 | P1. wasm_api 11모듈 → commands/(7개)+queries/(3개) 재분류. super::→super::super:: 경로 변경. 582테스트, WASM, Clippy 0 |
| 148 | 표 레이아웃 코드 통합 | 완료 | P1. table_layout+table_partial 중복 제거. 7개 공유 메서드 추출, 2,246→1,905줄(-341줄). 패딩 fallback 불일치 수정. 582테스트, WASM, Clippy 0 |
| 149 | Hexagonal Architecture 적용 | 완료 | P2. Core/Adapter 분리. DocumentCore 구조체 독립 모듈, HwpDocument Deref 래퍼, error.rs/model/event.rs 추출, 12파일 document_core/로 이동. 608테스트, WASM, Clippy 0 |
| 150 | Parser/Serializer Trait 추상화 | 완료 | P2. DocumentParser/DocumentSerializer trait + HwpParser/HwpxParser/HwpSerializer 구현체. parse_document()/serialize_document() 통합 진입점. from_bytes/export_hwp_native 마이그레이션. 모킹 테스트 4개. 586테스트, WASM, Clippy 0 |
| 151 | Event Sourcing + Batch Command | 완료 | P2. CQRS Stage 3. DocumentEvent 20종, begin/end batch(1회 paginate), paginate_if_needed 40개소, WASM 바인딩 3개. 594테스트, WASM, Clippy 0 |
| 152 | 테스트 커버리지 확대 | 대기 | P2. 도형·텍스트박스·각주·다단·캡션·표 분할 영역. 목표 ≥70% |
| 153 | TextMeasurer trait (플랫폼 격리) | 완료 | P2. TextMeasurer trait + 3구현체(Embedded/Wasm/Mock). #[cfg] 16→5개, build_cluster_len 중복제거, 호출부 0변경. 608테스트, WASM, Clippy 0 |

## 백로그

| No | 내용 | 발견일 | 상태 | 비고 |
|----|------|--------|------|------|
| B-006 | 다단 편집 설계 (pagination + DocumentPosition 확장) | 2026-02-18 | 미해결 | 현재 다단은 렌더링만 지원. 편집을 위해 줄 수준 단 분리(PartialParagraph), DocumentPosition에 column_index 추가, 커서/선택의 단 인식 등 전면 설계 필요 |
| B-007 | 그림자 텍스트 영역 편집/저장 시 오류 | 2026-02-21 | 미해결 | 그림자 효과가 적용된 텍스트에 글자를 추가 후 저장하면 오류 발생. 직렬화 시 변경된 비트 위치(bits 8-10/11-12/13/14)와 기존 raw_data 간 불일치 가능성 |
| B-008 | ~~텍스트 입력 후 표 생성 → 저장 시 파일 손상~~ | 2026-02-21 | 해결 | 타스크 133에서 수정. split_at() raw_header_extra 유실 + char_count 컨트롤 미포함 + 빈 문단 has_para_text 처리 |
