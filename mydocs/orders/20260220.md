# 오늘 할일 - 2026년 2월 20일

| No | 타스크 | 상태 | 비고 |
|----|--------|------|------|
| 119 | 글자모양 속성창 구현 및 속성 적용 기능 | 완료 | CharShapeMods 14개 속성 확장, WASM API 25+ 필드, 대화상자 UI(기본/확장 2탭), Alt+L 단축키, Aa 버튼. 571개 테스트 통과, WASM 빌드 성공. devel/main 머지 완료 |
| 120 | 글자 테두리/배경 렌더링 구현 + 수직 위치 보정 | 완료 | border_fill_id 파이프라인 연결(ResolvedCharStyle→TextRunNode→Rectangle/Line 노드), baseline 필드 추가로 텍스트 수직 위치 보정(bbox.y+baseline), SVG/Canvas 렌더러 수정, 대화상자 바인딩. 571개 테스트 통과 |
| 121 | 문단모양 설정 다이얼로그 UI 구현 | 완료 | 4탭 전체 구현. 기본(정렬/여백/첫줄/간격/미리보기), 확장(문단종류/기타옵션/세로정렬), 탭설정(탭종류/채움/위치/목록관리/자동탭/기본탭간격), 테두리/배경(선종류18개/굵기16개/색/미리보기+프리셋/배경면색·무늬/간격4방향). ParaShapeMods 22필드, TabDef·BorderFill 생성/재사용, 구역정의 파서 horizontal_align 필드 누락 수정. 571개 테스트 통과 |
| 122 | 문단 개요 번호/글머리표 렌더링 완성 | 완료 | Bullet 파싱(HWPTAG_BULLET)/렌더링, SectionDef.outline_numbering_id 파싱, Outline 구역수준 번호 렌더링, WASM 번호 카운터 리셋 버그 수정, 옛한글 자모 클러스터 렌더링/폭계산, diag 서브커맨드, 로컬빌드 문서화. 571개 테스트 통과, WASM 빌드 성공 |
| 123 | 벡터 품질 줌 구현 | 완료 | CSS transform 래스터 줌→Canvas ctx.scale() 벡터 재렌더링. WASM scale 파라미터, applyZoom 재작성(즉시 CSS+150ms 벡터 디바운스), 좌표변환/오버레이 줌 동기화, DPR 물리픽셀 1:1 매칭. 571개 테스트 통과, WASM 빌드 성공 |
| 124 | 글리프 패스 렌더링 (벡터 텍스트) | 중단 | 한컴 웹기안기 품질 미달 → 롤백. Canvas fillText + DPR 방식으로 전환 결정 |
| 125 | 폰트 메트릭 DB 생성 도구 구현 | 완료 | TTF 파싱(head/cmap/hmtx/name) → 582개 폰트 메트릭 DB 자동 생성. 한글 음절분해 압축(cho×jung×jong), WASM 내장 메트릭으로 JS 브릿지 호출 100% 제거. Noto CJK 포함. 571개 테스트 통과, WASM 빌드 성공 (1.83MB) |

## 백로그

| No | 내용 | 발견일 | 상태 | 비고 |
|----|------|--------|------|------|
| B-002 | 중첩 표 내부 셀에 캐럿 정확 위치 | 2026-02-17 | 완료 | DocumentPosition 확장 완료 |
| B-003 | 페이지 넘김 판단의 남은 공간·줄 높이 정확 측정 | 2026-02-17 | 완료 | HeightMeasurer 오차 해결 |
| B-004 | 비인라인 표 세로 위치 속성 | 2026-02-17 | 완료 | |
| B-006 | 다단 편집 설계 (pagination + DocumentPosition 확장) | 2026-02-18 | 미해결 | 현재 다단은 렌더링만 지원. 편집을 위해 줄 수준 단 분리(PartialParagraph), DocumentPosition에 column_index 추가, 커서/선택의 단 인식 등 전면 설계 필요 |
