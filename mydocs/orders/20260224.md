# 오늘 할일 - 2026년 2월 24일

| No | 타스크 | 상태 | 비고 |
|----|--------|------|------|
| 157 | 글상자 삽입 및 기본 편집 | 완료 | WASM API(생성/삭제/속성), 통합 개체 선택(image/shape), 마우스 드래그 생성, 8방향 핸들 크기조절, 이동(드래그/방향키), 삭제, 개체 속성 대화상자 글상자 탭(여백/세로정렬), Undo/Redo |
| 160 | 글상자 Z-Order 순서 변경 | 완료 | Rust change_shape_z_order_native(swap 방식) + WASM API + 4개 커맨드(맨 앞으로/앞으로/뒤로/맨 뒤로) + 우클릭 컨텍스트 메뉴. 새 글상자 생성 시 z_order=max+1 자동 할당. 한컴 호환 문단 재정렬은 미적용(스펙 미명시, 다음 타스크에서 실제 테스트 후 결정) |
| 161 | 글상자 Z-Order 한컴 호환성 검증 | 대기 | P0. 한컴에서 z_order 필드 vs 문서순서 중 어느 것을 렌더링에 사용하는지 실제 테스트. 필요 시 문단 재정렬(reorder_shape_paragraphs_by_z_order) 재도입 |
| 158 | 글상자 테두리/채우기/세로쓰기 | 대기 | P1. 테두리 선 종류/굵기/색, 면 채우기(색깔/그라데이션/그림), 세로쓰기(영문 눕힘/세움), 기호 대체(마침표→고리점 등), 회전(90도 단위), 도형→글상자 변환(도형 안에 글자 넣기/글상자 속성 없애기), 새 그리기 속성 등록 |
| 159 | 글상자 연결 및 고급 기능 | 대기 | P2. 글상자 연결(overflow→다음 글상자 자동 이어짐), 연결 해제, 연결 대상에 내용 있을 때 덮어쓰기, 하이퍼링크 속성 지정/해제, 개체 묶기/풀기, 정렬/배분 |
| 162 | 커스텀 탭 정지 및 탭 리더(채움 기호) 렌더링 | 완료 | 탭 위치 /2.0 스케일 보정, 왼쪽/오른쪽/가운데/소수점 탭 정렬, 크로스런 탭 처리, SVG/Canvas 탭 리더(실선/파선/점선) 렌더링, 바깥쪽/안쪽 쪽번호 위치 홀짝 처리, 구역 간 page_number_pos·쪽번호 연속 상속, Docker WASM 빌드 캐시 볼륨 |

## 백로그

| No | 내용 | 발견일 | 상태 | 비고 |
|----|------|--------|------|------|
| B-006 | 다단 편집 설계 (pagination + DocumentPosition 확장) | 2026-02-18 | 미해결 | 현재 다단은 렌더링만 지원. 편집을 위해 줄 수준 단 분리(PartialParagraph), DocumentPosition에 column_index 추가, 커서/선택의 단 인식 등 전면 설계 필요 |
| B-007 | 그림자 텍스트 영역 편집/저장 시 오류 | 2026-02-21 | 미해결 | 그림자 효과가 적용된 텍스트에 글자를 추가 후 저장하면 오류 발생. 직렬화 시 변경된 비트 위치(bits 8-10/11-12/13/14)와 기존 raw_data 간 불일치 가능성 |
| B-009 | PS/PCL 직접 생성 인쇄 엔진 (Renderer Trait 확장) | 2026-02-23 | 미해결 | HWP 모델 → PS/PCL 명령어 1:1 매핑. WASM 환경에서 드라이버 없이 IPP 직접 전송 및 인쇄 실현 (PoC), mydocs/tech/direct_printing_guideline.md |
