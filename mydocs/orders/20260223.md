# 오늘 할일 - 2026년 2월 23일

| No | 타스크 | 상태 | 비고 |
|----|--------|------|------|
| 149 | Hexagonal Architecture 적용 | 완료 | P2. Core/Adapter 분리. DocumentCore 구조체 독립 모듈, HwpDocument Deref 래퍼, error.rs/model/event.rs 추출, 12파일 document_core/로 이동. 608테스트, WASM, Clippy 0 |
| 152 | 테스트 커버리지 확대 | 대기 | P2. 도형·텍스트박스·각주·다단·캡션·표 분할 영역. 목표 ≥70% |
| 154 | HWPX 렌더링 고도화 | 완료 | P1. 3단계 완료: HWPX ctrl/문단 컨트롤 완전 파싱, renderingInfo 어파인 변환(개체묶기), Canvas/HTML 제어문자 필터링, 표 셀 baseline 보정(ensure_min_baseline). 608테스트·WASM 빌드 통과 |
| 155 | 표/셀 속성 대화상자 기본 탭 개편 | 완료 | 한컴 동일 UI 구현. 글자처럼취급/본문배치(어울림·자리차지·글뒤로·글앞으로)/가로·세로 위치/쪽영역제한/겹침허용/조판부호동기. 백엔드 attr 비트+raw_ctrl_data 11개 필드 get/set. 그룹 비활성 CSS 처리. 608테스트·WASM 통과 |
| 156 | 편집 모드 좌/우 여백 클리핑 제거 | 완료 | P1. 정렬 기반 간격 압축으로 해결. 양쪽정렬 음수 word spacing, 일본어(공백없음) 글자 간격 압축, 비정렬 오버플로우 균등 압축. 2패스 렌더링(오버플로우 컨트롤). Body/Cell clip 4px 여유. CSS 폰트 정밀도 개선. 608테스트·WASM 통과 |

## 백로그

| No | 내용 | 발견일 | 상태 | 비고 |
|----|------|--------|------|------|
| B-006 | 다단 편집 설계 (pagination + DocumentPosition 확장) | 2026-02-18 | 미해결 | 현재 다단은 렌더링만 지원. 편집을 위해 줄 수준 단 분리(PartialParagraph), DocumentPosition에 column_index 추가, 커서/선택의 단 인식 등 전면 설계 필요 |
| B-007 | 그림자 텍스트 영역 편집/저장 시 오류 | 2026-02-21 | 미해결 | 그림자 효과가 적용된 텍스트에 글자를 추가 후 저장하면 오류 발생. 직렬화 시 변경된 비트 위치(bits 8-10/11-12/13/14)와 기존 raw_data 간 불일치 가능성 |
| B-008 | ~~텍스트 입력 후 표 생성 → 저장 시 파일 손상~~ | 2026-02-21 | 해결 | 타스크 133에서 수정. split_at() raw_header_extra 유실 + char_count 컨트롤 미포함 + 빈 문단 has_para_text 처리 |
| B-009 | PS/PCL 직접 생성 인쇄 엔진 (Renderer Trait 확장) | 2026-02-23 | 미해결 | HWP 모델 → PS/PCL 명령어 1:1 매핑. WASM 환경에서 드라이버 없이 IPP 직접 전송 및 인쇄 실현 (PoC), mydocs/tech/direct_printing_guideline.md |
