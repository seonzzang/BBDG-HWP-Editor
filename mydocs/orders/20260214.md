# 오늘 할일 - 2026년 2월 14일

| No | 타스크 | 상태 | 비고 |
|----|--------|------|------|
| 64 | 웹폰트 매핑 및 시스템 폰트 폴백 연결 | 완료 | Rust generic_fallback() 함수로 serif/sans-serif 자동 분기, wasm-bridge.ts measureTextWidth에 폰트 치환 연결, style_resolver.rs에 8개 치환 항목 추가. 485개 Rust 테스트 통과, TS 빌드 성공 |
| 65 | 글상자(GSO TextBox) 커서 지원 | 완료 | CellContext 재사용(cell_index=0)으로 글상자 커서 진입/이동/탈출 구현. 방향키 글상자 진입(한컴 동작), hitTest 우선순위 수정, 테두리(실선/HWPUNIT 단위) 및 채우기(그라데이션) 렌더링, Canvas sin/cos 수정. 486개 Rust 테스트 통과, TS 빌드 성공 |
| 66 | 텍스트+Table 동시 포함 문단 렌더링 수정 | 완료 | pagination.rs에서 Table 뒤에 PartialParagraph(start_line=1) 배치. 표가 먼저 렌더링되고 텍스트가 표 아래에 올바르게 위치. 487개 Rust 테스트 통과, SVG 렌더링 확인 |
| 67 | rhwp-studio HWP 저장 기능 구현 | 완료 | web/ 프로젝트에서 포팅. wasm-bridge.ts에 exportHwp()/fileName 추가, file:save 커맨드 구현(Blob 다운로드), Ctrl+S 단축키 매핑. 텍스트 편집 후 저장 시 파일 손상 버그 수정(surgical_update_caret으로 DocInfo raw_stream 12바이트만 수정). 488개 Rust 테스트 통과, WASM/Vite 빌드 성공 |
| 68 | 비동기 웹폰트 2단계 로딩 완료 후 자동 리렌더링 트리거 | 완료 | loadWebFonts()가 backgroundDone Promise 반환, main.ts에서 완료 시 refreshPages() 자동 호출. Vite 빌드 성공 |
| 69 | 용지설정 윗여백·머릿말 높이 보정 (한컴 도움말 기준) | 완료 | PageAreas::from_page_def()에서 본문시작=margin_header+margin_top, 본문끝=height-margin_footer-margin_bottom으로 수정. 한컴 HWP와 동일한 여백 계산. 488개 Rust 테스트 통과, SVG 본문 시작 y=94.47px(기대값 94.49px) 확인 |
| 70 | 머리말/꼬리말 렌더링 및 쪽 번호 처리 | 완료 | pagination.rs에 HeaderFooterRef 구조체, 페이지별 Header/Footer 할당(Both/Even/Odd), PageNumberPos 수집. layout.rs에 Header/Footer 콘텐츠 렌더링(Table 포함 Footer 지원), format_page_number(아라비아/로마/원문자). 페이지별 문단 인덱스 기반 Footer 할당으로 Footer 덮어쓰기 버그 수정. 488개 Rust 테스트 통과, SVG 내보내기 정상 |
| 71 | 새 번호로 시작 (NewNumber) + 자동번호 체계 완성 | 완료 | AutoNumber 비트 오프셋 수정(format 8비트, superscript bit 12), 장식문자 파싱/직렬화, PageNumberPos 직렬화 수정. NewNumber→assign_auto_numbers 카운터 리셋, DocProperties 시작번호 초기화. PageContent.page_number + NewNumber(Page) 쪽 번호 반영. apply_auto_numbers에 format/장식문자 적용, format_page_number 중복 함수 제거. 488개 Rust 테스트 통과, WASM/Vite 빌드 성공 |
| 72 | 표 페이지 넘김 시 경계선·콘텐츠 중복 렌더링 수정 | 완료 | Body 노드에 clip_rect 필드 추가, SVG clipPath/Canvas clip() 적용. 인트라-로우 분할 최소 임계값(10px) 도입으로 미세 부분 행 방지. 488개 Rust 테스트 통과, WASM/Vite 빌드 성공 |
| 73 | 문단 부호 표시 기능 구현 | 완료 | ComposedLine.has_line_break + TextRunNode.is_line_break_end 플래그 추가, ¶→↵(U+21B5) 기호 변경, WasmBridge/커맨드/메뉴/툴바 토글 연결. 488개 Rust 테스트 통과, WASM/Vite 빌드 성공 |
| 73+ | 후속 수정: 빈 문단 기호 위치 + TextBox 인라인 이미지 + 도형 테두리 | 완료 | 빈 문단 ↵ 기호를 왼쪽 시작 위치에 배치, TextBox inline_y를 inner_area.y로 초기화하여 이미지 위치 수정, 도형 border attr bits 0-5=0(없음) 시 테두리 미렌더링. 488개 Rust 테스트 통과, 웹 검증 완료 |
| 74 | 개체 묶기(Group Shape) 파싱 및 렌더링 구현 | 완료 | ShapeObject에 Picture 변형 추가, 렌더링 변환 행렬(아핀 변환) 파싱으로 정확한 스케일/위치 계산, HorzRelTo::Page 본문 영역 기준 수정. 488개 Rust 테스트 통과, WASM/Vite 빌드 성공 |
| 75 | 표 앞 텍스트가 표 아래에 렌더링되는 문제 수정 | 완료 | pagination.rs에서 char_offsets 기반 접근을 표 CTRL_HEADER vertical_offset 기반으로 교체. vertical_offset > 0이면 자리차지 배치에서 모든 텍스트를 표 앞에 배치. 488개 Rust 테스트 통과, WASM/Vite 빌드 성공, 웹 검증 완료 |

## 백로그

| No | 내용 | 발견일 | 비고 |
|----|------|--------|------|
| B1 | ~~텍스트+Table 컨트롤 동시 포함 문단의 텍스트 렌더링 누락~~ | 2026-02-14 | **타스크 66으로 해결 완료** |
| B2 | ~~비동기 웹폰트 2단계 로딩 완료 후 자동 리렌더링 트리거~~ | 2026-02-14 | **타스크 68로 해결 완료** |
