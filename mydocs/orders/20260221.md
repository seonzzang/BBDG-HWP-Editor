# 오늘 할일 - 2026년 2월 21일

| No | 타스크 | 상태 | 비고 |
|----|--------|------|------|
| 125 | 폰트 메트릭 DB 생성 도구 구현 | 완료 | TTF 파싱(head/cmap/hmtx/name) → 582개 폰트 메트릭 DB 자동 생성. 한글 음절분해 압축(cho×jung×jong), WASM 내장 메트릭으로 JS 브릿지 호출 100% 제거. Noto CJK 포함. 571개 테스트 통과, WASM 1.83MB. devel/main 머지 완료 |
| 126 | Canvas DPR 스케일링 | 완료 | renderScale=zoom×DPR, canvas.style=물리픽셀/DPR, scale 상한 12.0, margin guides ctx.setTransform. 3파일 ~25줄 변경. 571개 테스트 통과, WASM 빌드 성공 |
| 127 | measureText LRU 캐싱 | 완료 | Rust 측 Vec 기반 LRU 캐시(256 엔트리) 도입. cached_js_measure() 래퍼로 WASM↔JS 브릿지 호출 결과 캐싱. measure_char_width_hwp + measure_hangul_width_hwp 2곳 적용. 571개 테스트 통과, WASM 빌드 성공 |
| 128 | Bold/Italic 폰트 폭 보정 | 완료 | MetricMatch 구조체로 Bold 폴백 감지. Regular 폴백 시 한컴 방식 보정(em_size+10)/20 적용. 2파일 ~20줄 변경. 571개 테스트 통과, WASM 빌드 성공 |
| 129 | 폰트 치환 체인 완전성 검증 | 완료 | 끊어진 체인 4건 수정(Gulimche→굴림체, 안상수체→함초롬돋움). Generic fallback 정규식 보완(palatino/georgia/batang/gungsuh). 571개 테스트 통과 |
| 130 | 문서별 동적 웹폰트 로딩 | 완료 | WASM getDocumentInfo()에 fontsUsed 추가. font-loader docFonts 기반 선별 로드 + loadedFiles 중복 방지. 상태바 진행률 표시. 5파일 ~40줄 변경. 571개 테스트 통과, WASM 빌드 성공 |
| 131 | 텍스트 변형 렌더링 (외곽선/그림자/양각/음각) | 완료 | outline_type 비트 파싱 수정(hwplib 참조, bits 8-10/11-12) + 양각/음각 파싱(bits 13/14). TextStyle 확장 + style_resolver 연결. Canvas 멀티패스 렌더링(그림자/외곽선/양각/음각). 8파일 ~125줄 변경. 571개 테스트 통과, WASM 빌드 성공 |
| 132 | 표 만들기 기능 구현 | 완료 | Rust createTable WASM API(커서 위치에 행×열 표 삽입, 문단 분할/빈 문단 교체 지원). 한컴 스타일 8×10 그리드 피커 + "표 만들기..." 상세 대화상자(줄/칸, 크기 지정, 기타). 메뉴+도구상자 양쪽 호출 가능. 한컴 blank_h_saved.hwp 기준 기본 속성값 적용(attr/패딩/정렬/높이 등). 빈 셀 캐럿 위치 폴백 개선. 7파일 ~750줄 변경. 571개 테스트 통과, WASM 빌드 성공 |
| 133 | 빈 문서 만들기 + 저장 | 완료 | 선결: 엔터키 버그 수정(split_at char_count 컨트롤 미포함, raw_header_extra 유실). 내장 템플릿(blank2010.hwp) 기반 새 문서 생성 + 문서 초기화 리팩터링 + file:new-doc 커맨드 활성화. 셀 나누기 후 저장 손상 수정(control_mask 재계산 + has_para_text 보정). 저장 시 파일이름 입력: showSaveFilePicker(Chrome/Edge) 우선 + 자체 대화상자 폴백. 셀 나누기 대화상자 + split_cell_into() 구현. 582개 테스트 통과, WASM 빌드 성공, TS 컴파일 성공 |
| 134 | CFB 오류 파일 복구 열기 | 완료 | "Malformed FAT (sector 0 pointed to twice)" 오류 파일을 LenientCfbReader 폴백으로 정상 열기. parse_hwp() strict→lenient 2단계 전략. LenientCfbReader에 read_file_header/section_count/read_body_text_section_full 추가. BinData lenient 로드 지원. 2파일 ~200줄 변경. 582개 테스트 통과, WASM 빌드 성공 |
| 135 | 표 셀 경계선 리사이즈 마커 표시 | 완료 | 셀 경계선 hover 시 row-resize/col-resize 커서 + 파란색 마커 표시. TableResizeRenderer 신규(경계선 좌표 계산+hitTest+마커 렌더링). InputHandler onMouseMove() 확장(RAF throttle+cellBboxes 캐싱). 3파일 ~210줄 변경. 582개 테스트 통과, WASM 빌드 성공, TS 컴파일 성공 |
| 136 | 표 크기 조절 기능 구현 | 완료 | Rust resizeTableCells 배치 WASM API + per-row 열 위치 레이아웃(build_row_col_x, 셀별 독립 너비 지원). 셀 선택 모드(F5): Ctrl+방향키 키보드 리사이즈 + 마우스 드래그 리사이즈 + 이웃 셀 보상(행 너비 유지). 일반 모드: 경계선 드래그로 열/행 전체 크기 변경. 셀 크기 변경 시 텍스트 리플로우(reflow_cell_paragraph) + 셀 선택 블럭 갱신. 3파일 ~350줄 변경. 582개 테스트 통과, WASM 빌드 성공, TS 컴파일 성공 |
| 137 | 표 키보드/마우스 이동 + Undo | 완료 | Rust moveTableOffset WASM API(오프셋 이동 + treat_as_char 문단 경계 교환 루프). 레이아웃 엔진 treat_as_char 표 h/v 오프셋 적용. 표 객체 선택 모드 방향키 이동(격자 크기 단위) + 마우스 드래그 이동. 표 외곽 클릭 선택. 격자 설정 대화상자. MoveTableCommand Undo/Redo(키보드 병합 + 드래그 일괄). 9파일 ~505줄 변경. 582개 테스트 통과, WASM 빌드 성공, TS 컴파일 성공 |
| 138 | 그림 삽입 기능 구현 | 완료 | Rust insertPicture WASM API(BinData 추가 + Picture 컨트롤 생성 + 문단 삽입). 마우스 영역 지정 모드(crosshair 커서 + 드래그 사각형 → 크기 지정 삽입). 클릭만 시 원본 크기 100%(열 폭 초과 시 비례 축소). Escape 취소. 입력 메뉴 + 도구 상자 양쪽 호출 가능. 5파일 ~425줄 변경. 582개 테스트 통과, WASM 빌드 성공, TS 컴파일 성공 |

## 백로그

| No | 내용 | 발견일 | 상태 | 비고 |
|----|------|--------|------|------|
| B-006 | 다단 편집 설계 (pagination + DocumentPosition 확장) | 2026-02-18 | 미해결 | 현재 다단은 렌더링만 지원. 편집을 위해 줄 수준 단 분리(PartialParagraph), DocumentPosition에 column_index 추가, 커서/선택의 단 인식 등 전면 설계 필요 |
| B-007 | 그림자 텍스트 영역 편집/저장 시 오류 | 2026-02-21 | 미해결 | 그림자 효과가 적용된 텍스트에 글자를 추가 후 저장하면 오류 발생. 직렬화 시 변경된 비트 위치(bits 8-10/11-12/13/14)와 기존 raw_data 간 불일치 가능성 |
| B-008 | ~~텍스트 입력 후 표 생성 → 저장 시 파일 손상~~ | 2026-02-21 | 해결 | 타스크 133에서 수정. split_at() raw_header_extra 유실 + char_count 컨트롤 미포함 + 빈 문단 has_para_text 처리 |
